export const calculateCost = (req, res) => {
    // const { distance, volume } = req.body;
    // if (!distance || !volume) {
    //     return res.status(400).json({ error: 'Missing parameters' });
    // }
    const cost = "It Works!"; // Пример расчёта
    res.json({ cost });
};
